# http-security-test

A simple tool to validate the security of any given set of HTTP response headers. Can be invoked from the command line or imported as a Python module.

**NOTE: This is work in progress. Stay tuned!**
